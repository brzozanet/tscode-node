// NOTE: CommonJS

// const notifications = {
//   success: (text) => console.log(`Sukces! ${text}`),
// };

// module.exports = notifications;

// NOTE: ES module

export const notifications = {
  success: (text) => console.log(`Sukces! ${text}`),
};
